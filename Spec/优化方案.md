# RimFacilityCompat 优化技术方案

## 0. 优化目标

保持当前“三态声明式注入、运行时即时生效、不落盘改 Def”语义不变，重点降低：

1. 启动扫描时间：当前最坏约 `O(K·q·N)`，主要来自未缓存的 `ResolveType`；
2. 启动注入时间：当前即使无配置也全量跑 `ApplyInjection`；
3. 单次 UI 改动延迟：当前每次单击都“全类别重建 + 全地图重连”；
4. 反向索引重建范围：当前会重建“旧∪新全部链接设施”，而不是只重建增删设施；
5. 配置窗口每帧 `ShouldLink` 的线性查找。

建议分阶段实施，每阶段可独立回归。

***

## 0.5 现状核对说明（2026-09-09 代码审计后补充）

本方案撰写时对数据模型的描述需按以下现状修正，其余假设与 `Source` 现实现一致：

- **已不存在独立的** **`excludedTargets`** **黑名单字典**。Settings 只持久化 `linkedTargets`（1.6/Source/Data/Settings.cs），三态语义内嵌其中：**无 key = 未配置（回退 originalLinks）；有 key 且空列表 = 显式全断开；有 key 且非空 = 显式启用集合**。方案的"黑名单"操作（DisableAllTargets 等）写入的即是空列表态，无需新增字典。
- `originalLinks` / `categories` 不落盘，每次启动由 `FacilityPatcher.Apply()` 阶段一扫描重建。
- 所有"仅单个 key"的操作（如断开某条链接）都会**物化**出对应 key（从 originalLinks 拷贝），因此 `linkedTargets.Count == 0` 严格等价于"无任何用户显式配置"。
- UI 与注入现状（**此条记录的是 2026-09-09 撰写时的状态，P4/P5 落地后已不适用，仅作方案动机依据保留**）：`ApplyInjection` 无范围参数、返回 int；`RelinkSpawnedThings()` 无参全地图遍历；任何改动经 `Dialog_FacilityMatrix.ApplyAndSave` 全量执行（对应 §4/§5 的优化动机成立）。

***

## 0.6 实施状态（2026-09-12 更新）

**P1–P11 均已落地**。下文各章节保留撰写时的「方案 / 建议」口吻与代码骨架，作为设计依据留存；实际签名与实现以 `1.6/Source` 为准。

| 阶段 | 状态 | 落地位置 |
|---|---|---|
| P1 `ResolveType` 字典缓存 | ✅ | `FacilityScanner.typeCache` / `ResetTypeCache`（`ScanAll` 开头重建） |
| P2.1 连通分量改 BFS | ✅ | `FacilityScanner.AddGenericBuildingGroups` |
| P2.2 去重不再用 `List.Contains` | ✅ | `FacilityPatcher.ScanOriginalLinks` |
| P3 无配置时跳过注入 | ✅ | `FacilityPatcher.Apply`（`settings.linkedTargets.Count == 0`，跳过前提已在注释中钉死） |
| P4.1 引入 `InjectionResult` | ✅ | `FacilityPatcher.InjectionResult`（`Applied` + `ChangedTargets`） |
| P4.2 `ApplyInjection` 范围参数 | ✅ | `ApplyInjection(settings, onlyCategory, onlyTargetDefName)`；旧 `int` 签名未保留 |
| P4.3 反向索引只重建增删设施 | ✅ | `FacilityPatcher.AddAffectedByDiff`（对称差） |
| P4.4 `FacilityListEquals` 改 O(n) | ✅ | `FacilityPatcher.FacilityListEquals`（HashSet 比较） |
| P5 只重连变化的目标 | ✅ | `FacilityPatcher.RelinkSpawnedThings(IEnumerable<ThingDef>)`；无参重载未保留 |
| P6 `ShouldLink` HashSet 缓存 | ✅ | `Settings.linkedSetCache` / `InvalidateLinkedSet`，`ExposeData` 读档分支清空 |
| P7 启动计时埋点 | ✅ | `FCDebug.TimeScope` + `FacilityPatcher.Apply` 各阶段（扫描 / 原始链接 / 快照 / 注入 / 落盘） |
| P8 预设窗口导出折叠 | ✅ | `Dialog_PresetManager.sourceExpanded` + 来源限高滚动区 |
| P9 设施池收录过滤 | ✅ | `FacilityScanner.validFacilityNames` / `GetValidLinkedFacilities` |
| P10 配置界面诊断日志 | ✅ | `Dialog_FacilityMatrix.LogClick`、`FC.LogMatrixTabSwitch`、`GridHitMap.TryHitGeometry` |
| P11 类别 Tab 布局缓存 | ✅ | `Dialog_FacilityMatrix.tabRects` + 重算期间固定 `Text.Font = GameFont.Small` |

> 门控机制已随实现调整：`FCDebug.DebugBuild` 现由 `#if DEBUG` 条件编译决定（Debug 编译恒 `true`、Release 编译恒 `false`），不再是下文 §7.1 所述的"发布态手动置 `false`"。

***

## 1. Phase 1：给 `ResolveType` 加“会话级字典缓存”

### 问题

`FacilityScanner.ResolveType()` 每次对每个已知类别都全量扫一遍 `DefDatabase<ThingDef>.AllDefs`，并且结果不缓存。

### 方案

在 `FacilityScanner` 内加缓存，并在每次 `ScanAll()` 开头清空。

```csharp
private static Dictionary<string, Type>? typeCache;

private static void ResetTypeCache()
{
    typeCache = null;
}

private static Type? ResolveType(string className)
{
    if (typeCache == null)
    {
        typeCache = new Dictionary<string, Type>();

        foreach (var def in DefDatabase<ThingDef>.AllDefs)
        {
            var type = def.thingClass;
            if (type != null && !typeCache.ContainsKey(type.Name))
                typeCache[type.Name] = type;
        }
    }

    return typeCache.TryGetValue(className, out var result) ? result : null;
}
```

在 `ScanAll()` 第一行调用：

```csharp
public static Dictionary<string, CategoryInfo> ScanAll()
{
    ResetTypeCache();
    // ... 原逻辑
}
```

### 效果

- 每次扫描只构建一次 `Type.Name → Type` 字典；
- `ResolveType` 从 `O(q·N)` 降到 `O(1)`；
- 玩家增删 Mod 后重启，`ScanAll()` 会清空并基于新 DefDatabase 重建，无过期问题。

***

## 2. Phase 2：优化启动扫描的其它显式瓶颈

### 2.1 普通 Building 连通分量改用邻接表 BFS

当前 `AddGenericBuildingGroups` 里每个目标都会 `remaining.Where(...)` 扫描剩余目标，最坏 `O(B²·d)`。

改为一次性建立“设施 defName → 目标”索引，再 BFS：

```csharp
var facilityToTargets = new Dictionary<string, List<ThingDef>>();

foreach (var target in genericTargets)
    foreach (var facility in targetLinks[target])
    {
        if (!facilityToTargets.TryGetValue(facility.defName, out var list))
            facilityToTargets[facility.defName] = list = new List<ThingDef>();
        list.Add(target);
    }

var remaining = new HashSet<ThingDef>(genericTargets);

while (remaining.Count > 0)
{
    var first = remaining.First();
    remaining.Remove(first);

    var componentTargets = new List<ThingDef> { first };
    var componentFacilities = new Dictionary<string, ThingDef>();
    var queue = new Queue<ThingDef>();
    queue.Enqueue(first);

    while (queue.Count > 0)
    {
        var target = queue.Dequeue();

        foreach (var facilityDef in targetLinks[target])
            componentFacilities[facilityDef.defName] = facilityDef;

        foreach (var facilityDef in targetLinks[target])
        {
            if (!facilityToTargets.TryGetValue(facilityDef.defName, out var candidates))
                continue;

            foreach (var candidate in candidates)
            {
                if (remaining.Remove(candidate))
                {
                    componentTargets.Add(candidate);
                    queue.Enqueue(candidate);
                }
            }
        }
    }

    // 生成 category，逻辑同原代码
}
```

复杂度从最坏 `O(B²·d)` 降到 `O(B·d + B)`。

### 2.2 `ScanOriginalLinks` 去重阶段不要用 `List.Contains`

当前对同一个 key 反复 `Contains`，最坏接近 `O(L²)`。

用临时 `HashSet` 构建，最后再转 `List`：

```csharp
public static Dictionary<string, List<string>> ScanOriginalLinks(...)
{
    var result = new Dictionary<string, List<string>>();

    // ...原有外层循环...

    // 用一个局部 HashSet 去重后一次性转 List
    foreach (var group in temp.GroupBy(...))
        result[key] = group.Select(x => x.defName).Distinct().ToList();

    return result;
}
```

更简单做法：

```csharp
if (!result.TryGetValue(key, out var set))
{
    set = new HashSet<string>();
    result[key] = new List<string>(); // 或最后统一转换
}
```

如果你后续把 `originalLinks` 类型改成 `Dictionary<string, HashSet<string>>`，这一步也可以彻底省掉 List。

***

## 3. Phase 3：启动时无配置则跳过全量 `ApplyInjection`

### 问题

当前启动阶段即使 `linkedTargets` 为空，也全量执行一次 `ApplyInjection`，遍历“所有类别 × 所有目标 × 所有设施”，只是为了确认“无需修改”。

### 方案

在 `FacilityPatcher.Apply()` 里判断：

```csharp
if (settings.linkedTargets.Count == 0)
{
    // 没有用户显式配置：ShouldLink 全部回退 originalLinks，
    // 当前 Defs 已经是 XML 原始状态，不需要重建。
    applied = 0;
}
else
{
    applied = ApplyInjection(settings).Applied;
}
```

注意：`linkedTargets.Count == 0` 包括“首次运行 ResetToOriginal 后”的干净状态，正好覆盖最常见的无配置启动路径。

### 实施要求

1. **只跳** **`ApplyInjection`，阶段一（ScanAll / ScanOriginalLinks / SnapshotOriginalComps）必须照常执行**：`targetsWithOriginalComp` 启动快照与 `originalLinks` 是后续一切注入/孤儿清理的基准，即使无配置也要在启动时重建（现 `Apply()` 阶段一无条件执行，保持不动）。
2. **跳过逻辑依赖“defs 恒为 XML 原始状态”前提**（由运行时注入不落盘保证）：在跳过分支注释中钉死此前提，并同步守卫“落盘改 Def / 进程内热重载 / 第三方改 def”等会破坏前提的扩展点。

### 效果

- 无配置时启动阶段省掉整个 `ApplyInjection`；
- 有配置时才全量重建，保留三态语义。

***

## 4. Phase 4：`ApplyInjection` 改为“可限定类别/目标”并返回变更集合

### 问题

现在一次单击也会遍历所有类别，且 `RelinkSpawnedThings()` 无法知道哪些目标变了，只能全图扫。

### 4.1 引入返回结构

```csharp
public sealed class InjectionResult
{
    public int Applied;
    public List<ThingDef> ChangedTargets = new();
}
```

### 4.2 添加可选范围参数

```csharp
public static InjectionResult ApplyInjection(
    FacilityCompatSettings settings,
    string? onlyCategory = null,
    string? onlyTargetDefName = null)
{
    var result = new InjectionResult();
    var affectedFacilities = new HashSet<ThingDef>();

    foreach (var kvp in settings.categories)
    {
        if (onlyCategory != null && kvp.Key != onlyCategory)
            continue;

        // 原逻辑...
        foreach (var targetDef in GetTargetDefs(info))
        {
            if (onlyTargetDefName != null &&
                targetDef.defName != onlyTargetDefName)
                continue;

            // 计算 finalList、判断 changed。changed 出口共 4 处，统一收 ChangedTargets：
            //   ① 原版 comp 清空（声明为空且原本有链接）
            //   ② 自建 comp 整体移除（声明为空；无条件计——空列表自建 comp 也移除）
            //   ③ 新建 comp（原无 comp 且 finalList 非空）
            //   ④ 写入 finalList（内容有变化）
            // 每处出口同步 result.Applied++
        }
    }

    // 重建受影响设施的反向索引
    foreach (var facilityDef in affectedFacilities)
        facilityDef.GetCompProperties<CompProperties_Facility>()?
            .ResolveReferences(facilityDef);

    return result;
}
```

旧 `int ApplyInjection(settings)` 签名的调用者共 3 处（`Apply()` 启动注入 + 两个 UI `ApplyAndSave`），随本阶段全部迁移到新签名后**不保留旧入口转发**——无调用者的公共 API 是维护负担。

### 4.3 反向索引只重建“实际增删”的设施

当前代码把“旧列表∪新列表”的所有设施都加入 `affectedFacilities`。
例如目标原本连了 50 个设施，只断开 1 个时，会重建 50 个设施的反向索引。

改为只加入对称差集：

```csharp
if (oldList != null)
    foreach (var f in oldList)
        if (!finalList.Contains(f))
            affectedFacilities.Add(f);

foreach (var f in finalList)
    if (oldList == null || !oldList.Contains(f))
        affectedFacilities.Add(f);
```

更稳妥可用 `HashSet<ThingDef>` 比较：

```csharp
static void AddAffectedByDiff(
    HashSet<ThingDef> output,
    List<ThingDef>? oldList,
    List<ThingDef> newList)
{
    var oldSet = oldList == null
        ? new HashSet<ThingDef>()
        : new HashSet<ThingDef>(oldList);

    foreach (var f in newList)
        if (!oldSet.Contains(f))
            output.Add(f);

    var newSet = new HashSet<ThingDef>(newList);
    if (oldList != null)
        foreach (var f in oldList)
            if (!newSet.Contains(f))
                output.Add(f);
}
```

这样单击一条链接通常只重建 1\~2 个设施的反向索引，而不是几十个。

### 实施要求

1. **`ResolveReferences`** **单次成本 = O(全 Defs)**（原版引擎 virtual，override 内部全量扫 `AllDefsListForReading` 重建 `linkableBuildings`）。对称差把受影响设施缩到“实际增删”集合（断 1 条链 → 1\~2 个）：有配置启动总成本 ≈ 受影响设施数 × O(全 Defs)，按此量级设预期；UI 单击场景 1\~2 次可接受。
2. **对称差 old 基准 = 运行时 def 现值**：现状实现 `oldList = comp?.linkableFacilities` 已直接读 def 现值（2026-09-09 调研确认），保持该行不动即满足本要求。二次注入（启动注入后 UI 局部注入）时 def 现值已含首次注入结果，以此作基准保证与一次全量注入的结果一致。

### 4.4 `FacilityListEquals` 改成 O(n)

当前：

```csharp
foreach (var f in oldList)
    if (!newList.Contains(f))
        return false;
```

这是 O(n²)。改为先建一次 HashSet：

````csharp
private static bool FacilityListEquals(
    List<ThingDef>? oldList,
    List<ThingDef> newList)
{
    if (oldList == null || oldList.Count != newList.Count)
        return false;

    if (oldList.Count == 0)
        return true;

    var set = new HashSet<ThingDef>(newList);
    foreach (var f in oldList)
        if (!set.Contains(f))
            return false;

    return true;
}

### 实现说明

- `FacilityListEquals` 用 HashSet 比较，丢失**顺序与重复**语义；对 `linkableFacilities` 安全，因为引擎 `PotentialThingsToLinkTo` 按距离排序、不依赖列表序。在实现注释中钉死此前提。

### 实现说明（2026-09-09 调研核实，P4+P5 捆绑实施依据）

1. **一个目标 def 只归属一个类别**（Scanner 的已知类 thingClass 匹配与 Building 连通分量均为单归属划分）：`onlyCategory` 注入时 ChangedTargets 必在限定类别内。跨类别共享的只有设施 def，其反向索引经 `ResolveReferences` 全量反算（引擎按全 defs 现值重建 `linkableBuildings`）天然覆盖其他类别现状——§7.3 要求 3（跨类别共享设施）有结构性保证，无需额外交互。
2. **类别级 `allFacilityDefs` 不因 `onlyTargetDefName` 而跳过**：finalList = 类别全部设施 ∩ ShouldLink，目标过滤后仍需完整类别设施集；目标过滤只放在内层目标循环头部。
3. **changed 出口 4 处已逐一核实**（见 4.2 骨架注释），其中 ②（自建 comp 整体移除）无条件计数——孤儿清理按 ChangedTargets 过滤后靠它命中存量实例。

---

## 5. Phase 5：UI 只重连真正发生变化的目标

### 方案

`RelinkSpawnedThings` 增加按目标 Def 过滤的重载：

```csharp
public static int RelinkSpawnedThings(IEnumerable<ThingDef> changedTargets)
{
    if (Current.Game == null)
        return 0;

    var targetSet = new HashSet<ThingDef>(changedTargets);
    if (targetSet.Count == 0)
        return 0;

    int relinked = 0;

    foreach (var map in Find.Maps)
    {
        foreach (var targetDef in targetSet)
        {
            foreach (var thing in map.listerThings.ThingsOfDef(targetDef))
            {
                var comp = thing.TryGetComp<CompAffectedByFacilities>();
                if (comp == null)
                    continue;

                // 原有“孤儿 comp 清理/Notify_ThingChanged”逻辑
            }
        }
    }

    return relinked;
}
````

`map.listerThings.ThingsOfDef(targetDef)` 已确认存在（public，O(1) 字典返回），直接用；仅当未来版本 API 变动时再退回全图 `AllThings` 并按 `targetDef.defName` 过滤。

### UI 调用

`Dialog_FacilityMatrix.ApplyAndSave` 改为带范围：

```csharp
private void ApplyAndSave(
    string? onlyCategory = null,
    string? onlyTarget = null)
{
    var result = FacilityPatcher.ApplyInjection(
        settings, onlyCategory, onlyTarget);

    FacilityPatcher.RelinkSpawnedThings(result.ChangedTargets);
    settings.Write();
}
```

各入口：

| 操作                                  | 建议作用域               |
| ----------------------------------- | ------------------- |
| 单击切换一条连接                            | 当前类别 + 当前目标 defName |
| 退出批量模式                              | 当前类别                |
| 重置单个设施                              | 当前类别                |
| 重置单个主设施                             | 当前类别 + 该主设施         |
| 复制/粘贴到单个设施                          | 当前类别                |
| 复制/粘贴到单个主设施                         | 当前类别 + 该主设施         |
| “全部启用/禁用”且选主设施                      | 当前类别 + 该主设施         |
| “全部启用/禁用”且选设施                       | 当前类别                |
| 应用/导入预设（Dialog\_PresetManager 两处调用） | 全量（一次性操作，可接受）       |

### 实施要求（正确性）

1. **用** **`map.listerThings.ThingsOfDef(def)`** **取实例**（public，O(1) 字典返回），不要用全图 `AllThings` 遍历。
2. **`ChangedTargets`** **收集覆盖全部 changed 出口**：在判定 changed 的**每个出口**（含“自建 comp 整体移除”分支）统一 `result.ChangedTargets.Add(targetDef)`——孤儿清理分支按 ChangedTargets 过滤后，才能命中这类目标的地图存量实例（先逐个 `CompFacility.Notify_LinkRemoved` 解旧登记、清 `LinkedFacilitiesListForReading`、再移除实例 comp）。
3. **只重连目标 def 实例即可闭环**：目标实例 `Notify_ThingChanged → RelinkAll` 经 `Notify_NewLink / Notify_LinkRemoved` 反向修正设施实例登记。**跨类别共享设施**（设施 F 同时可连类别 A、B 的目标，仅注入 A）须专门回归：验证 F 的放置指示（读 `linkableBuildings`）与实例登记正确。
4. **无参** **`RelinkSpawnedThings()`** **不保留**（2026-09-09 调研确定）：预设管理器两处调用走全量注入 + `result.ChangedTargets` 重连（未变目标无需重连），无参全图 `AllThings` 扫描版无存在必要。UI 现有 7 处 `ApplyAndSave` 调用点（Dialog\_FacilityMatrix 5 处 + Dialog\_PresetManager 2 处）全部迁移到带范围版本。

***

## 6. Phase 6：`ShouldLink` 的 HashSet 缓存

### 问题

`ShouldLink` 使用 `List.Contains`，配置窗口每帧对可见格子调用，白名单大时线性查找累积明显。

### 方案

保留 `linkedTargets` 的 `List` 作为序列化格式，同时维护内存 HashSet 缓存。

在 `Settings` 内：

```csharp
private Dictionary<string, HashSet<string>> linkedSetCache = new();
```

```csharp
public bool ShouldLink(
    string category,
    string facilityDefName,
    string targetDefName)
{
    var key = MakeKey(category, facilityDefName);

    if (linkedTargets.TryGetValue(key, out var linked))
        return GetLinkedSet(key).Contains(targetDefName);

    return originalLinks.TryGetValue(key, out var orig)
        && orig.Contains(targetDefName);
}

private HashSet<string> GetLinkedSet(string key)
{
    if (linkedSetCache.TryGetValue(key, out var set))
        return set;

    set = new HashSet<string>(linkedTargets[key]);
    linkedSetCache[key] = set;
    return set;
}

private void InvalidateLinkedSet(string key)
    => linkedSetCache.Remove(key);
```

所有修改 `linkedTargets` 的地方都调用 `InvalidateLinkedSet(key)`：

- `ToggleLink`
- `SetLink` 最终经由 `ToggleLink` 或直接赋值时
- `EnableAllTargets`
- `DisableAllTargets`
- `SetLinkedList`
- `ResetToOriginal`：清空整个缓存
- `ResetFacilityToOriginal`：移除对应缓存
- `ResetTargetToOriginal`：移除对应缓存
- `SetTargetLinkedList` / `EnableAllTargets`（含“类别失效写空列表”分支）/ `DisableAllTargets` / `SetLinkedList`：按 key 失效
- `ExposeData`：须区分读写分支——**读档分支后清空整个缓存**；写档分支不清（启动 `FacilityPatcher.Apply` 内 `settings.Write()` 也会触发 ExposeData，若写档即清缓存将导致启动注入期间反复重建）

如果 `originalLinks` 某些 key 的 List 很长，也可以做同样的惰性 HashSet，但建议先只给用户可变的 `linkedTargets` 加，避免提高常驻内存。

***

## 7. Phase 7：增加可量化验证

> **阶段定位（2026-09-09 修订）：本阶段应提前为 Phase 0 执行**——先量化现状（启动扫描/注入 ms、单次 UI 单击 ms）再逐阶段优化，每阶段完成后复测对比。

### 7.1 启动计时/内存埋点

埋点挂在现有 FCDebug 门控上（1.6/Source/Utils/Debug/FCDebug.cs；`DebugBuild` 由 `#if DEBUG` 条件编译决定，Release 编译恒 `false`，即全短路）。

计时统一用 `FCDebug.TimeScope("标签")` 作用域（using 包裹，Dispose 输出毫秒耗时）。
**注意门控是编译期** **`DebugBuild`** **而非运行时** **`EnableLog`**：启动计时发生在主菜单出现前，
用户无法打开菜单开关，挂 `EnableLog` 会丢失全部启动数据；`DebugBuild = false` 时返回空单例，零开销：

```csharp
using (FCDebug.TimeScope("启动·声明式注入 ApplyInjection"))
{
    // ScanAll / ApplyInjection ...
}
```

### 7.2 回归验证

优化后需要验证：

1. 无配置启动：所有目标 `linkableFacilities` 与 XML 原始状态一致；
2. 有配置启动：配置被正确重建；
3. 运行时单击连接/断开：仅对应目标被重连，且已放置建筑即时生效；
4. 重置/导入预设：语义不变；
5. 卸载某 Mod 后重启：缓存清空、缺失 defName 被静默跳过；
6. 同一进程内重复触发 `ScanAll`（如果未来加“重新扫描”）时结果正确。

### 7.3 补充回归清单

在 7.2 之外，逐阶段必须额外验证：

1. **二次注入不漂移**（§4.3 要求 2）：启动有配置注入后，UI 内再局部注入同一类别/目标，反向索引结果与“一次全量注入”完全一致；
2. **孤儿实例可清理**（§5 要求 2）：对“已整体移除自建 comp”的目标，局部注入后其地图上存量实例被正确解链并移除实例 comp；
3. **跨类别共享设施**（§5 要求 3）：仅注入类别 A 时，同连 A/B 两类的设施 F 的放置指示（`linkableBuildings`）与实例登记正确。

### 7.4 实施顺序

P7/Phase 0（先测量）→ P1 → P2 → P3（启动路径，独立可回归）→ **P4 + P5 捆绑**（UI 路径，互为前置，捆绑实施避免中间态不可用）→ P6（微优化，最后）。

***

## 8. Phase 8：预设窗口导出折叠与布局加固（UI）

> **阶段定位（2026-09-10 新增）**：P1–P7 均为启动/运行时性能优化；本阶段是与性能链路解耦的 UI 修复与交互精简，可独立实施、独立回归。动因来自实际使用观察——导出场景绝大多数是**全量导出**，细分来源属低频操作，现状却让它们常驻平铺、挤压版面。

### 问题

现状 `Dialog_PresetManager`（1.6/Source/UI/Dialog_PresetManager.cs）的 `DoWindowContents` 把“全部来源 + 每个 Mod 来源”按钮常驻平铺：

1. **版面被平铺按钮占用**：高频的全量导出与低频的细分多选地位相同，干扰识别、增加误操作；
2. **来源 wrap 行数无上限**：来源按钮按行换行，导出区高度随 Mod 数量线性增长；
3. **导入区被压缩甚至挤出**：导入区高度按“窗口底部固定预留（全局工具）− 当前 y”的剩余空间计算；当来源按钮约 9 行以上时，导出区高度逼近或超过该剩余空间，导入列表高度趋 0/负、全局工具被推出窗口——即用户实测到的“导入连接页面被压缩”。

### 方案

保持导出语义不变：折叠行左侧为**全部来源状态钮**（点击仅切换范围、不执行导出），真正执行导出的仍是行尾独立“导出”按钮；默认折叠，来源细分按钮仅展开后出现，并放入**限高滚动区**。该滚动区同时将导出区高度变为有界值，从根上消除问题 2/3。

#### 8.1 折叠 / 展开两态

新增窗口实例状态字段：

```csharp
/// <summary>来源细分区是否展开（默认折叠，聚焦全量导出）</summary>
private bool sourceExpanded;
```

**折叠态（默认）**，位于文件名行上方，仅一行：

- `[全部来源]` 状态钮：`allSelected == true` 时高亮（复用现状 `SelectAllSources` / `allSelected` 语义）；若当前已选部分来源（`allSelected == false`），文案随状态显示 `已选 {0} 个来源`，保证折叠后用户仍能看清导出范围，且可直接点击一键回全量；
- `[展开 ▸]` 按钮：置 `sourceExpanded = true`。

**展开态**：

- 首行：`[全部来源]` 状态钮 + `[收起 ▾]` 按钮（置 `sourceExpanded = false`）；两者都位于滚动区之外，点击可立即 `return`；
- 其下为来源按钮滚动视图，高度固定为 `SourceAreaH ≈ SourceBtnH * 5 + RowGap * 2`，超出部分滚动——Mod 数量再多也不撑高导出区；
- 各 Mod 来源按钮的单选/多选切换逻辑（`ToggleSource` / `ChosenSources`）与现有一致。

#### 8.2 布局约束

- 折叠态：导出区高度恒等于（来源行 1 行 + 文件名行 + 预览行），不再随 Mod 数量变化；
- 展开态：导出区在折叠高度上追加固定值 `SourceAreaH`，仍为有界值；
- 导入区维持现有“剩余空间分配”，此时导出区上限已知，导入区不再被压没，全局工具行位置恢复稳定；
- 滚动区高度 / 滚动条宽度沿用现有布局常量（`ScrollBarW`、`RowGap` 等），不新增魔法数字。

#### 8.3 滚动区内点击不得提前 `return`

来源按钮移入 `BeginScrollView` 后，点击分支若直接 `return`（现状代码如此），会跳过 `EndScrollView`，触发引擎 “Mouse position stack is not empty” 报错。按导入列表既有模式（`DrawImportList`），点击动作延迟到 `EndScrollView` 之后执行：

```csharp
Action? pendingSourceAction = null;
Widgets.BeginScrollView(sourceAreaRect, ref sourceScroll, sourceContentRect);
foreach (var source in allSources)
{
    // 仅收集动作，不在滚动区内 return
    if (Widgets.ButtonText(new Rect(...), source))
        pendingSourceAction = () => ToggleSource(source);
}
Widgets.EndScrollView();
pendingSourceAction?.Invoke();
```

折叠态无滚动，可保留立即 `return` 的旧写法；两态各占一个绘制分支。

### 实施要求

1. 仅修改 `1.6/Source/UI/Dialog_PresetManager.cs` 的导出区绘制并新增状态字段；`FacilityPreset` / `Settings` / `ChosenSources` / `DoExport` 语义不动；
2. 来源按钮区（折叠 1 行 / 展开 1 行 + 滚动区）始终绘制在文件名行之前的同一纵向顺序；
3. 新增翻译键：`FC.ExportExpand`（展开 ▸）、`FC.ExportCollapse`（收起 ▾）、`FC.ExportPartialFmt`（`已选 {0} 个来源`，折叠态文案）；
4. 展开/折叠为会话级 UI 状态，不落盘、不加配置项；
5. 位于滚动区内的任何按钮（含将来的收起钮若移入区内的变体）点击一律延迟执行，禁止提前 `return`。

### 验收

1. 来源 Mod ≥ 10 个时窗口布局不再被撑开，导入列表与全局工具保持可见可用；
2. 折叠态点击独立“导出”= 全量导出，与现状行为一致；
3. 展开选择部分来源后折叠，状态钮显示“已选 n 个来源”；再次展开勾选状态不回退；
4. 展开态点击任意来源按钮无控制台报错（验证 Begin/EndScrollView 配对）。

***

## 9. Phase 9：设施池收录过滤——预设往返零跳过（数据正确性）

> **阶段定位（2026-09-10 新增）**：数据正确性修复，与 P1–P7 性能链路、P8 UI 布局均相互独立。
> 现象：同一会话内导出预设后立即原地导入，提示「跳过 N 条」（测试时为 25 条），玩家误以为配置丢失。

### 问题

根因是**扫描收录设施与导入校验设施的「合法设施」标准不一致**：

- **收录侧**（`FacilityScanner`）：类别设施池来自目标 `CompProperties_AffectedByFacilities.linkableFacilities` 的引用（`GetValidLinkedFacilities`），只过滤 null，**不检查被引用者是否带 `CompProperties_Facility`**；`ScanOriginalLinks` 遍历同一份引用列表，同样照单全收；
- **导入侧**（`FacilityPreset.Import` 校验 2）：设施 def 必须存在**且**带 `CompProperties_Facility`，否则跳过计数。

`linkableFacilities` 是目标侧的 XML 引用声明，引擎并不强制被引用者真的是设施。各 mod 中的误引用/死引用（被引用 def 无设施 comp，运行时永远不会建立连接）由此一路流入：

1. 类别设施池（`CategoryInfo.facilities`）；
2. `originalLinks`（原始链接扫描原样记录）；
3. `ShouldLink` 对其返回 true（未配置时原始链接兜底）；
4. `CollectLinks` 将其作为启用连接导出；
5. 导入校验 2 判定「非设施」→ skipped++。

同会话原地往返时，导入校验 1（目标 def 存在）、校验 3（目标与设施同属一个类别池）在结构上必然通过——目标与设施都来自同一份内存 `settings.categories`，故 skipped 全部来自校验 2。

**附带现象**：这些死项同样出现在配置矩阵 UI 中，玩家可勾选但永不生效（设施侧无 `CompFacility`，蓝图不画线、建成不连接）。

### 方案

在**扫描收录侧单点过滤**，让「设施池」语义严格等于「带 `CompProperties_Facility` 的可连接设施」：

1. `FacilityScanner.GetValidLinkedFacilities`：在现有 Where 链追加 `facilityDef.GetCompProperties<CompProperties_Facility>() != null`（与 `HasFacilityComp` 同一判定）。死引用不进 `targetLinks` → 不进任何类别设施池、不参与普通建筑连通分量建边、不计入 `referencedFacilities`（死设施自身无 comp，本就不在 `facilityDefs` 中，不会误入待分配区）；
2. `FacilityPatcher.ScanOriginalLinks`：遍历 `linkableFacilities` 时同样跳过无 comp 设施，避免 `originalLinks` 残留永不被查询的死 key；
3. `FacilityPreset.Import` 跳过日志分级（`FCLogger.Msg` 普通信息级别，**不用 `Warn`**——跳过是预期内静默行为，警告会在玩家控制台制造误报）：
   - **发行版**：导入结束只输出**一条汇总**——按五类原因（目标为空 / 目标 def 缺失 / 设施 def 缺失 / 设施存在但非可连接设施 / 无共同类别）给出计数分布，避免订阅 mod 较少的玩家导入大整合包预设时被逐条缺项日志淹没；
   - **调试版**（`FCDebug.DebugBuild` 门控）：逐条输出 Target/Facility 的 defName 详情；其中「设施存在但无 `CompProperties_Facility`」单独成条，文案直接说明最常见成因——mod 作者引用方向写反或者把不是设施的def声明为连接设施，该连接运行时本就不会生效；
   - 玩家侧仍由既有消息汇总提示「已导入 X 条，跳过 Y 条」，交互语义不变。

**为何不选「导出侧 `CollectLinks` 过滤」**：那只能让往返计数归零，UI 矩阵中的死设施项、`originalLinks` 冗余、注入候选集噪声仍然存在；扫描侧过滤是单点改动、语义彻底——设施池干净后，导出集合、UI 矩阵、`ApplyInjection` 的 finalList 候选集一并干净。

#### 9.1 实现约定（零重复判定）

`ScanAll` 中 `facilityDefs`（全部带 `CompProperties_Facility` 的 def）先于 `targetLinks` 构建，直接由它生成合法设施名集合，过滤走 O(1) 字典查找而非逐条扫 comps：

```csharp
var facilityDefs = allDefs.Where(HasFacilityComp).ToList();
// 合法设施 defName 集合：与 HasFacilityComp 同源，GetValidLinkedFacilities 按此 O(1) 过滤
var validFacilityNames = new HashSet<string>(facilityDefs.Select(d => d.defName));
var targetLinks = affectedDefs.ToDictionary(
    def => def,
    def => GetValidLinkedFacilities(def, validFacilityNames));
```

`GetValidLinkedFacilities` 在现有 Where 链追加 `validFacilityNames.Contains(facilityDef.defName)`；`ScanOriginalLinks` 同理按「`GetCompProperties<CompProperties_Facility>() != null`」跳过（该方法只遍历类别内目标，直接判定即可，不另建集合）。

### 性能影响

- **启动扫描**：新增一次 HashSet 构建（数百项，一次性）+ 每条 `linkableFacilities` 引用一次 O(1) 查找，总量在毫秒以下；可用既有 `FCDebug.TimeScope("启动·扫描 ScanAll")` 埋点前后对比；
- **原始链接扫描**：同为一次性遍历；死引用剔除后 `originalLinks` 条目数减少，内存与查询范围小幅下降；
- **运行时（每帧 UI / 单击注入 / 导出）**：零新增成本，且设施池更小——矩阵图标列、`ApplyInjection` finalList 候选集、`CollectLinks` 枚举项均小幅缩减，方向全部为正；
- 过滤为 Where 链惰性求值，不引入中间物化集合，无分配放大。

### 实施要求

1. 过滤条件统一以「`CompProperties_Facility` 存在」为准，与 `HasFacilityComp`、导入校验 2 同一标准，不引入第二套判定；
2. 扫描侧过滤按 9.1 复用 `facilityDefs` 构建 HashSet，不在引用遍历中重复扫 comps；
3. 不放宽导入三道校验——跨安装缺 def 时跳过仍是正确且必要的行为；
4. 跳过日志走 `FCLogger.Msg`（普通信息级别、中文），不使用 `Warn`/`Err`：发行版只输出一条按原因分类的计数汇总，逐条 defName 详情挂 `FCDebug.DebugBuild` 门控；
5. 不改变预设文件格式（v2）：旧预设若含死项，导入时依旧跳过（行为不变）；修复后新导出的预设不再产生死项。

### 验收

1. 同一会话全量导出后立即导入：**skipped = 0**；
2. 配置矩阵中不再出现「勾选后永不生效」的无 comp 设施项；
3. 卸载某 mod 后导入含其 def 的预设：仍正常跳过，且日志列出缺失的 defName 与原因；
4. 启动注入后，所有「有效设施」的连接状态与修复前完全一致（死项本就不产生实际连接，修复仅清理不可达条目）。

***

## 10. Phase 10：配置界面诊断日志（UI 可观测性）

> **阶段定位（2026-09-10 新增）**：纯可观测性改动，不改动任何交互与绘制行为；与 P1–P9 独立。

### 问题

有玩家反馈「配置界面无法点击」，但现有日志无法定位：

- `OnCellClick` 的打点只在**命中成功**时输出，且受运行时菜单开关门控——未命中时完全静默，拿不到任何证据；
- 更关键的是**自锁**：一旦界面真的不可点击，玩家也打不开同一个窗口里的「调试日志」开关，依赖运行时开关的诊断路径在此场景下必然失效。

### 方案

全部日志挂在**编译期常量** `FCDebug.DebugBuild` 下（写法 `if (FCDebug.DebugBuild) FCLogger.Msg(...)`，与 Phase 9 的逐条跳过详情同一模式）：
Debug 编译输出、Release 编译整体消失。玩家侧由作者提供 debug 包，**无需任何 UI 开关**，从根上规避自锁。

1. **初始化日志（3 条，一次性）**
   - `Mod.DoSettingsWindowContents` 实际创建窗口时：记录窗口栈规模；
   - `Dialog_FacilityMatrix` 构造函数：记录类别数、目标总数、设施总数、当前类别、Mod 目录——类别/目标数为 0 即「扫描结果为空」，界面自然无可点击项；
   - 首帧绘制末尾（`RouteMouse` 之后）：记录内容区矩形与两栏**已注册命中格子数**（主设施/附属设备）——均为 0 说明可点击区域为空。此日志位于路由之后，格子数即「实际可点击项数」。
2. **点击诊断日志（每次左键按下至多 1 条，命中/未命中均记录）**

   单行给全判定链事实，键 `FC.LogMatrixClick`：

   - `事件`：`Event.current.type`，识别点击是否被 `Layout` 等非鼠标事件抢先消费；
   - `窗口局部 / 全局 / 窗口`：三者自洽即坐标变换正常。「窗口局部」按 `全局 − 窗口位置 − Margin` 换算（`DoWindowContents` 的内容原点在窗口内缩 `Window.Margin` 处，早期打点漏减 Margin，导致该字段与绘制/命中坐标系恒差 18px，反而制造出假的「错位」证据）；
   - `窗口内 / 整窗命中`：`Mouse.IsOver` 整窗矩形的结果。整窗命中=0 而鼠标在窗口内 ⇒ 输入被上层窗口拦截（`IsInputBlockedNow`），此时界面**必然整体无响应**，直接对应「点 Tab/点格子都没反应」的症状；
   - `区域`：`M`=主网格 / `A`=附属网格 / `W`=窗口其他（Tab、按钮、搜索行）/ `O`=窗口外。用于区分「点在网格上却没命中」与「本来就点在别处」——后者（例如点 Tab）不是缺陷；
   - `几何命中 / 引擎命中`：裸 `rect.Contains`（全局空间）与 `Mouse.IsOver` 的结果。两者不一致 ⇒ 输入被拦截或坐标系不同源；均为 `-` ⇒ 纯几何未命中。

   坐标一律取自 `UI.MousePositionOnUIInverted`（`Event.current.mousePosition` 在不同事件阶段取值可能不一致，故只作对照、不作判据；旧打点只用了它，无法与绘制坐标对照，实测会出现推算窗口位置互不相同的矛盾值）；对拍统一在**全局 UI 空间**，窗口局部矩形平移 `contentOrigin = windowRect.position + Window.Margin`（与 `DrawDebugOverlay` 青色十字同一换算）。
3. **类别 Tab 点击日志**：`FC.LogMatrixTabSwitch` 记录 `旧类别 → 新类别｜按钮={rect} 鼠标={Event.current.mousePosition}`，用于区分「输入没到窗口」与「到了但状态没变」——Tab 走引擎 `Widgets.ButtonText`，若它也不触发，则基本可判定整窗输入被拦截（与上面的 `整窗命中=0` 互相印证）；新增的 `rect`/`鼠标` 两字段可直接判定命中判定与绘制是否同源（鼠标不在被点 Tab 的矩形内即为错位实证）。
4. `GridHitMap` 新增 `TryHitGeometry(Side, Vector2 globalUIPos, Vector2 contentOrigin, out string)`：仅做 `rect.Contains`（全局 UI 空间），不含遮挡/焦点检查，专供上述对拍使用；正常交互仍一律走 `TryHit`（保留遮挡判定，防穿透）。配套新增诊断计数属性 `MainCount` / `AccessoryCount`（首帧日志用）。

### 实施要求

1. 一律走 `if (FCDebug.DebugBuild) FCLogger.Msg(key, args)`，**不使用运行时开关** `FCDebug.Log`——界面不可点击时玩家无法打开开关，会自锁；
2. 点击日志沿用 `RouteMouse` 既有的 `Time.frameCount != lastClickFrame` 帧级去重，保证一次点击至多一条；
3. 桌面鼠标坐标**只允许来自 `UI.MousePositionOnUIInverted`**；任何诊断比较都必须在同一坐标空间内完成（窗口局部一律经 `windowRect.position` 平移换算），禁止用 `Event.current.mousePosition` 作判据；
4. 点击日志**不做区域过滤**（每次左键按下记一条，命中/未命中都记），区域归属由 `区域` 字段表达，避免过滤掉关键证据（首版按「仅记网格区域内」过滤，结果现场一条都留不下）；
5. 不改动任何交互/绘制逻辑，只新增日志与只读查询（`TryHitGeometry` 不参与正常路由）；
6. 中文日志，与现有风格一致。

### 验收

1. **Debug 编译**：打开配置界面，依次出现「打开入口 / 初始化数据规模 / 首帧格子数」三条日志；
2. **Debug 编译**：每次左键按下有且仅有一条 `FC.LogMatrixClick`，命中格子时 `引擎命中` 为该格 defName；
3. **Debug 编译**：点击类别 Tab ⇒ 出现 `FC.LogMatrixTabSwitch`（旧→新）；若未出现且 `FC.LogMatrixClick` 中 `区域=W`，说明点击到了 Tab 但引擎未处理；
4. **Debug 编译**：界面整体无响应时，`FC.LogMatrixClick` 应呈现 `窗口内=1` 且 `整窗命中=0`，即输入被上层窗口拦截（此时无响应属引擎行为，非本 mod 缺陷）；
5. **Debug 编译**：在网格区域内点击图标间隙 ⇒ `区域=M/A`、`几何命中=-`，属正常未命中；
6. **Release 编译**：以上日志全部不出现，零开销、零噪声。

***

## 11. Phase 11：类别 Tab 命中错位修复（UI 稳定性）

> **阶段定位（2026-09-11 新增）**：只消除 Tab 布局的非确定性并补正诊断坐标系，不改动命中机制与交互语义。

### 问题

玩家反馈「鼠标点击后，响应的 Tab 按钮在光标右侧一段距离」——即看到的是 A，生效的是右边的 B。

### 成因

Tab 的宽度由 `Text.CalcSize(displayName).x + TabPadX` 决定，而 `Text.CalcSize` 依赖**全局可变状态** `Text.Font` / `Text.FontSize` 与字体图集。其返回值在帧之间、乃至**同帧的不同事件阶段**（`Layout` / `MouseDown` / `Repaint`）都可能出现细微差异。只要某一个 Tab 的宽度变了：

1. 该行的换行点跳变；
2. 换行点之后**整行 Tab 横向重排**（后续行的起点随之改变）；
3. 用户按上一帧（Repaint）看到的位置点击，而 `Widgets.ButtonText` 在 `MouseDown` 阶段用的是重排后的 rect ⇒ 命中落在该位置的另一个 Tab。

旁证：代码中既有的布局振荡检测（`Dialog_FacilityMatrix.lastViewY`）注释已记录 `viewRect.y` 实测在 126 ↔ 158 间交替——两者恰为 **2 行 / 3 行 Tab**（差 32 = `TabH + TabGap`），说明 Tab 行数确实在帧间跳变，即换行点不稳定。

### 方案

1. **Tab 布局缓存**（`tabRects` + `tabLayoutKeys` / `tabLayoutWidth` / `tabLayoutY`）：仅当类别集合或可用宽度（起始 y 一并纳入）变化时重算；绘制与命中**永远共用同一组 rect**，且跨帧、跨事件阶段恒定。
2. **重算时固定字体** `Text.Font = GameFont.Small`（保存/恢复，`finally` 恢复）：与 `Widgets.ButtonText` 绘制文字所用字体同源，避免「按钮框宽 ≠ 实际文字宽」。
3. **诊断坐标系补正**：`DoWindowContents` 运行在 `GUI.BeginGroup(winRect.ContractedBy(Window.Margin))` 内，其内容原点在窗口内缩 `Margin` 处。`LogClick` 与 `DrawDebugOverlay` 此前漏减 `Margin`，导致「窗口局部」与青色十字**恒差 18px**，反而制造出假的「错位」证据；`GridHitMap.TryHitGeometry` 的参数语义同步由 `windowOffset` 正名为 `contentOrigin`。
4. **Tab 命中日志增强**：`FC.LogMatrixTabSwitch` 追加被点 Tab 的 `rect` 与命中时的 `Event.current.mousePosition`，鼠标不在该 rect 内即「命中判定与绘制不同源」的实证。

### 实施要求

1. 缓存失效条件必须同时覆盖「类别集合变化」与「可用宽度变化」（窗口缩放后仍正确）；
2. 绘制与命中必须使用同一组缓存 rect，禁止任何一侧单独重算；
3. 不改动命中机制（仍走 `Widgets.ButtonText`），只消除布局的非确定性；
4. 字体固定仅限布局计算期间，必须 `finally` 恢复，禁止污染其他绘制。

### 验收

1. 打开配置界面后，Tab 行数在一次会话内恒定（不再 2 ↔ 3 行跳变）；
2. 单击任一 Tab，`FC.LogMatrixTabSwitch` 中的 `鼠标` 坐标落在 `按钮` 矩形内（错位消除的判定依据）；
3. 缩放窗口后 Tab 立刻按新宽度重排，此后保持稳定；
4. 开启 `FCDebug.EnableDraw` 时，品红（`Event.current.mousePosition`）与青色（全局换算）十字重合，不再恒差 18px；
5. Release 编译零日志、零开销。

***

## 12. 预期效果

| 阶段                 | 优化前                  | 优化后                          |
| ------------------ | -------------------- | ---------------------------- |
| `ResolveType`      | 最坏 `O(K·q·N)`        | 每次扫描 `O(N)` 构建一次字典，之后 `O(1)` |
| 普通 Building 聚类     | 最坏 `O(B²·d)`         | `O(B·d + B)`                 |
| 无配置启动              | 全量类别矩阵重建             | 跳过注入                         |
| 单击一次链接             | 全类别重建 + 全图扫描         | 单目标重建 + 单目标类型重连              |
| 反向索引重建             | 旧∪新全部设施              | 只重建增删设施                      |
| `ShouldLink` UI 查询 | `List.Contains` O(n) | 用户覆盖表走 HashSet O(1)          |
| 预设窗口导出区（P8） | 来源按钮平铺，来源多时挤压导入/全局 | 折叠 + 来源限高滚动，导出区高度有界 |
| 设施池收录（P9）   | 无 comp 死引用混入设施池，往返导入误报跳过、UI 现死项 | 收录即过滤，设施池只含可连接设施，往返 skipped=0 |
| 配置界面诊断（P10） | 未命中点击静默、日志开关自锁，问题无法复现定位 | debug 包输出初始化/点击对拍日志，Release 零开销 |
| 类别 Tab 布局（P11） | 宽度随 `Text.CalcSize` 抖动，换行点漂移致点击命中相邻 Tab | rect 缓存 + 字体同源，绘制与命中恒定一致 |

整体期望是：

- 无配置启动：从“全量扫描 + 全量注入”降到“一次全 Def 扫描 + 建索引 + 分类”，最坏增长从接近二次压到接近线性；
- 有配置启动 / UI 操作：从“全量类目矩阵”变为“按变更目标/类别局部重建”；
- 预设窗口（P8）：全量导出默认零额外点击，来源细分在展开后的限高滚动区中选择，窗口在任何 Mod 数量下不被撑开；
- 设施池与预设（P9）：配置矩阵、导出预设、注入候选集只含真正可连接的设施，原地导出→导入 skipped 恒为 0，跨安装缺 mod 的跳过有中文日志可查；
- 配置界面诊断（P10）：debug 包可区分「扫描为空 / 可点击区为空 / 鼠标不在格子 / 输入被拦截」四类成因，Release 无任何日志开销；
- 类别 Tab 布局（P11）：Tab 的 rect 一次算出、跨帧与跨事件阶段恒定，点击「所见即所中」；诊断坐标与绘制同源，不再出现 18px 的假错位。

P1–P7 不改变 RimFacilityCompat 的现有用户可见行为，也不会引入存档格式不兼容；Phase 8 仅改变预设窗口的默认布局（默认折叠、展开才显示细分来源），导出与连接语义不变；Phase 9 仅清理「本就永远不会生效」的死引用条目，对有效设施的连接状态无任何改变，预设文件格式（v2）不变；Phase 10 仅新增 debug 编译可见的日志与只读查询，不改交互行为，Release 编译下整体消失；Phase 11 仅消除类别 Tab 布局的帧间不确定性并补正诊断坐标换算，命中仍走引擎 `Widgets.ButtonText`，交互语义不变。
